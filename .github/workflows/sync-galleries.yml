name: Sync galleries from Cloudinary

on:
  schedule:
    # Une fois par jour à 5h UTC. Déclenchement manuel possible à tout
    # moment via workflow_dispatch (bouton "Run workflow" sur GitHub).
    - cron: "0 5 * * *"
  workflow_dispatch:
    inputs:
      cloudinary_folder:
        description: "Dossier racine Cloudinary (ex. karinebauzin)"
        required: false
        default: karinebauzin
        type: string

concurrency:
  group: sync-galleries-${{ github.event.inputs.cloudinary_folder || 'karinebauzin' }}
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Vérifier les secrets Cloudinary
        env:
          CLOUDINARY_CLOUD_NAME: ${{ secrets.CLOUDINARY_CLOUD_NAME }}
          CLOUDINARY_API_KEY: ${{ secrets.CLOUDINARY_API_KEY }}
          CLOUDINARY_API_SECRET: ${{ secrets.CLOUDINARY_API_SECRET }}
        run: |
          missing=0
          for var in CLOUDINARY_CLOUD_NAME CLOUDINARY_API_KEY CLOUDINARY_API_SECRET; do
            if [ -z "${!var}" ]; then
              echo "::error::Secret manquant : $var"
              missing=1
            fi
          done
          if [ "$missing" = 1 ]; then
            echo ""
            echo "Ajoute dans GitHub → Settings → Secrets → Actions :"
            echo "  CLOUDINARY_CLOUD_NAME"
            echo "  CLOUDINARY_API_KEY"
            echo "  CLOUDINARY_API_SECRET"
            echo "  CLOUDINARY_FOLDER  (optionnel, défaut karinebauzin)"
            exit 1
          fi

      - name: Générer et uploader _galleries.json
        env:
          CLOUDINARY_CLOUD_NAME: ${{ secrets.CLOUDINARY_CLOUD_NAME }}
          CLOUDINARY_API_KEY: ${{ secrets.CLOUDINARY_API_KEY }}
          CLOUDINARY_API_SECRET: ${{ secrets.CLOUDINARY_API_SECRET }}
          CLOUDINARY_FOLDER: ${{ github.event.inputs.cloudinary_folder || secrets.CLOUDINARY_FOLDER || 'karinebauzin' }}
        run: node scripts/sync-galleries.mjs

      - name: Commit manifest local (secours)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/_galleries.json
          git diff --staged --quiet || git commit -m "chore: sync manifest galeries Cloudinary"
          git push

      - name: Rappel vérification
        env:
          CLOUDINARY_CLOUD_NAME: ${{ secrets.CLOUDINARY_CLOUD_NAME }}
          CLOUDINARY_FOLDER: ${{ github.event.inputs.cloudinary_folder || secrets.CLOUDINARY_FOLDER || 'karinebauzin' }}
        run: |
          echo "Vérifie le manifest :"
          echo "  curl -s https://res.cloudinary.com/${CLOUDINARY_CLOUD_NAME}/raw/upload/${CLOUDINARY_FOLDER}/_galleries.json | head"
          echo ""
          echo "Après upload de nouvelles photos sur Cloudinary, relance ce workflow ou attends le cron (5 h UTC)."
